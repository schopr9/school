<%= form_for(@class_group) do |f| %>
  <% if @class_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@class_group.errors.count, "error") %> prohibited this class_group from being saved:</h2>

      <ul>
      <% @class_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :class_name %><br>
    <%= f.text_field :class_name %>
  </div>
  <div class="field">
    <%= f.label :year %><br>
    <%= f.number_field :year %>
  </div>
  <div class="field">
    <%= f.label :student_id %><br>
    <%= f.number_field :student_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
